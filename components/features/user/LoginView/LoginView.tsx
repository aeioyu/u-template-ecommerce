import React, { useState } from 'react';

interface Props {}

const LoginView: React.FC<Props> = (props) => {
  return (
    <div>
      <div>username</div>
      <div>password</div>
    </div>
  );
};

export default LoginView;
